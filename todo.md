# Ahadu Deploy core batch

- [x] Define the `ahadu.json` schema, validation rules, and deployment arguments.
- [x] Add local folder and ZIP project input flows.
- [x] Implement deterministic detection from `package.json`, `composer.json`, `artisan`, `requirements.txt`, and common entry files.
- [x] Generate framework-specific entry points, Wasmer configuration, and Ahadu Deploy command arguments.
- [x] Validate the core flows, save a checkpoint, push the public repository, and pause before GitHub OAuth.
- [ ] Deferred: implement GitHub OAuth and authorization only after explicit user approval.
